# HerdFlow backend — local dev ($0). Copy to .env.local and adjust.
# Local Postgres (Docker):  docker run -e POSTGRES_PASSWORD=dev -p 5432:5432 postgres:16
DATABASE_URL=postgresql://postgres:dev@localhost:5432/postgres
